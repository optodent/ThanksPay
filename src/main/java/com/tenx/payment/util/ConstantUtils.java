package com.tenx.payment.util;

public class ConstantUtils {

    // The scale (number of digits after decimal point) for BigDecimal types
    public static final int DECIMAL_DIGITS_SCALE = 10;

    // Number of total digits for BigDecimal
    public static final int DECIMAL_DIGITS_PRECISION = DECIMAL_DIGITS_SCALE + 10;
}
